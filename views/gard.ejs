<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ried gard</title>
    <link rel="stylesheet" href="css/anime.css">
    <link rel="stylesheet" href="css/index.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
      .autocomplete-suggestions-faculty {
         width: 100%;
          border: 1px solid #ddd;
          max-height: 150px;
          overflow-y: auto;
          background-color: #fff;
          position: absolute;
          z-index: 9999;
          display: none; /* เริ่มต้นซ่อน */
      }
      .autocomplete-suggestion-faculty {
          padding: 10px;
          cursor: pointer;
      }
      .autocomplete-suggestion-faculty:hover {
          background-color: #f0f0f0;
      }
      .autocomplete-suggestions {
         width: 100%;
          border: 1px solid #ddd;
          max-height: 150px;
          overflow-y: auto;
          background-color: #fff;
          position: absolute;
          z-index: 9999;
          display: none; /* เริ่มต้นซ่อน */
      }
      .autocomplete-suggestion {
          padding: 10px;
          cursor: pointer;
      }
      .autocomplete-suggestion:hover {
          background-color: #f0f0f0;
      }
  </style>
</head>
<body>
    <%- include('navbar')%>
    <div class="conteaner">
        <form class="form" method="POST" action="/ingard" >
            <h1>gard</h1>
            <div class="input-container">
                <input required="" id="input" type="text"  name="name" />
                <label class="label" for="input">ชื่อ(ไม่ระบุตัวตน)</label>
                <div class="underline"></div>
            </div>
              <div class="input-container">
                <input required="" id="input" type="text" name="cm"/>
                <label class="label" for="input">coment</label>
                <div class="underline"></div>
              </div>
              <div class="d-flex ">
              <button type="submit" id="myButton">
                SEND
              </button>
            </div>
            </form>
           
</div>
</body>
      <script src="js/drop.js"></script>
  </html>